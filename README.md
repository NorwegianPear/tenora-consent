# tenora-consent

Static confirmation page shown to a customer's Global Administrator after the Microsoft admin-consent step for Tenora Device. Pure static HTML: stores nothing, calls nothing, collects nothing. Source of truth: tenora-device-beta `docs/consent-landing/index.html`.
